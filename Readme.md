Array is the list of element of the same type placed in a contiguous memory Location

## Important
“Java is strictly pass-by-value. For arrays, the reference is passed by value, allowing modification of elements but not reassignment.”

OPERATION IN ARRAY

1. Create
2. Input
3. Output
4. Update


1. CREATING AN ARRAY

datatype  arrayname[] = new datatype [size]
ex. int arr[] = new arr[5];


2. INPUT
int arr[0] = sc.nextInt();

3. OUTPUT
System.out.print (arr[0]);

4. UPDATE
arr[0] = 100;
System.out.print (aar[0]);

